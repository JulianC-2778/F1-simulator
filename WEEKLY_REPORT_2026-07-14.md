# 模块四周报：赛道信息预读取与地图辅助驾驶（2026-07-14）

## 一、背景：上周状态与助教反馈

上周汇报时，AI bot 完全依靠 SCR 协议提供的 **19 束激光测距传感器**（最远约 200 米）临场反应驾驶。助教提出：**现实中的赛车手都是提前研究赛道的，为什么 bot 不能预读取赛道信息，而要临场反应？**

会后重新核查了技术边界，结论是：**"只准用传感器"是 SCR 国际竞赛的比赛规则，不是协议的技术限制，更不是本项目的要求。** TORCS 每条赛道的完整几何数据公开存放在赛道 XML 文件中（等同于人类车手赛前拿到的赛道图；内置 bot 则是直接读取引擎内存，包括路面摩擦系数等物理真值，属于更高权限）。因此预读取完全可行——本周据此完成了整套"地图辅助驾驶"系统。

## 二、本周完成的工作

### 1. 新增赛道模型模块 `track_model.py`

| 关键函数 | 作用 |
|---|---|
| `TrackModel.from_xml(path)` | 解析赛道 XML：按顺序读出每个路段（直道 `lg` 长度；弯道 `radius` 半径 + `arc` 弧度，含渐变半径弯），以及赛道宽度 |
| `_corner_limit_ms(r)` | 弯道极限速度的物理模型：`v = √(A_LAT·R / (1 − K_AERO·R))`。分母是**下压力项**——速度越快下压力越大、抓地越强，所以大半径快弯几乎不限速，发卡弯几乎不受影响（低速时下压力小，物理上自洽） |
| 取直修正（构造函数内） | XML 给的是中心线半径，但车手走"外-内-外"取直：`R_有效 = R + W·cos(α/2)/(1−cos(α/2))`，α 为弯道总偏转角。小折角有效半径放大数百米（全油门通过），发卡弯加成≈0。**同方向相邻弯段先合并**再算 α，防止一个 90° 大弯被 XML 拆成多段小弧后误判为可取直 |
| 倒序刹车扫描（构造函数内） | 沿整圈每 2 米采样，先算各点弯道极限，再倒序传播刹车约束：`v[i] = min(v[i], √(v[i+1]² + 2·A_BRAKE·ds))`，即"现在就要慢到能应付前方一切"。跨起跑线回绕扫两圈保证闭环正确 |
| `limit_kmh(dist)` | 运行时 O(1) 查表：给出"距起跑线 dist 米处的最高安全速度"（取相邻两个采样的较小值，容忍 1~2 米对齐误差） |
| `calibrate(measured)` | **练习圈里程表校准**：XML 几何在渐变弯多的赛道上有 ~1% 累积误差（Forza 算出 5850 米，实际 5784 米），圈末弯道位置会偏几十米。以实测圈长为准整体缩放所有查询，一次消除累积漂移 |
| `next_corner(dist)` | 前方第一个真实弯道的距离/方向/半径/限速（已被取直的弯不报告）——下周喂给 Granite 的"赛道简报"接口 |
| `line_tpos(dist)` | 走线目标横向位置：距弯 250 米内建议占该弯**外侧**（外-内-外的入弯占位） |
| `load_track_model('auto')` | 零配置加载：读取 `~/.torcs/config/raceman/` 最新比赛配置，自动匹配玩家在游戏菜单选择的赛道 |

解析精度验证：g-track-2 解析圈长 3186 m vs 官方 3185.8 m（误差 0.2 m）。

### 2. 接入驾驶控制（`ai_bot.py` 的 `compute_control()`）

采用**双层设计**，确保地图失效时行为退回纯传感器、绝不更危险：

- **保守层（min）**：`目标速度 = min(传感器目标, 地图限速)` ——地图只有"提前刹车"的权力。用于为传感器视距（200 米）外的弯道提前减速；
- **信任层（max，五道闸全过才激活）**：`目标速度 = max(传感器目标, 地图限速×0.97)`。五道闸：① 练习圈校准已完成；② 车在线上且对正；③ 前方 100 米内无对手（地图看不见交通）；④ 传感器视距与地图预言的下弯距离自洽（防地图不知道的障碍）；⑤ 当地限速 ≥150 km/h（**慢弯和弯心永不信任，完全归传感器**）。作用：消除传感器在直道上的**假刹车**（0° 波束擦边误判视距，基线日志每圈 6~7 次 200+ km/h 下无故点刹），并按精确刹车点**晚刹车**；
- **刹车点模式**：当地图刹车曲线决定目标速度时，全油门顶到刹车点、压线后强响应重刹（模仿真实车手），替代原来"提前 180 米渐松油滑行"的保守节奏；
- **入弯走线**：`line_tpos` 建议的占位目标经**斜率限制**（每帧最多 0.02）后注入转向的持线项，弯前平滑滑向外侧；该项随 pursuit 激活自动淡出，弯中转向仍 100% 由传感器控制；
- **运行时保险**：`distFromStart` 超出地图圈长 2% 即判定装错赛道、自动弃图；`safety_filter`、撞车/脱轨/逆向恢复、ABS/TCL 等原安全体系全部不变。

### 3. 工程方法（本周同样重要的部分）

- **A/B 开关**：`--track off` 一键回到纯传感器基线；
- **量化仪表**：每圈打印 `[lap] 圈时 / map-bound%（地图接管帧占比）/ trust%（信任模式帧占比）`，所有标定用数字说话；
- **无图形测试台**：`torcs -r` 纯文本跑完整比赛。验证了仿真是**确定性**的（GUI 与无图形基线圈时一致到 0.01 秒），此后回归测试不再依赖人工盯屏；
- **标定教训**（两条，均已固化为代码注释）：① 地图必须**标定偏乐观**——有 min() 兜底，乐观的图顶多不生效，悲观的图整圈拖后腿（第一版恒定 1g 模型比不读图还慢，正是此因）；② 无图形与 GUI 环境**稳定性不等价**——一个微分阻尼项在无图形下更平顺、GUI 下因渲染卡顿延迟反而蛇形，已回滚（结论：观感类改动必须 GUI 终审）。

### 4. 最终结果（Forza，同配置 3 圈 A/B，确定性仿真）

| | 第 2 圈 | 第 3 圈 | 车损 |
|---|---|---|---|
| 纯传感器（上周形态） | 113.14 s | 113.16 s | 0 |
| **地图辅助（本周形态）** | **111.48 s** | **111.41 s** | **0** |

**每圈快 1.7 秒**；信任模式活跃于约 30% 的帧；练习圈校准实测圈长 5784 m（与官方值一致）。

### 5. 对助教问题的正面回答

bot 现在的完整流程与真实车手同构：**赛前研究赛道图（解析 XML）→ 第一圈跑"练习圈"校准里程表 → 正赛中在确认安全的路段信任赛前功课（晚刹车、走线），在复杂路段信任眼睛（传感器）**。"提前了解赛道"的价值已由 A/B 数据量化：1.7 秒/圈。

## 三、下周计划（P2：强化 Granite 的作用）

1. 把 `next_corner()` 的赛道简报、剩余圈数、与前后车差距趋势喂入 Granite 策略 prompt——让 LLM 拿着全局信息做战略（进攻/防守/进站时机），而不是只看眼前 200 米；
2. Granite 决策与理由实时上屏（复用已有 overlay 模块）+ 决策日志，赛后可回放"AI 为什么这么决定"；
3. 适度扩大 Granite 决策权（进站圈选择、激进度微调），保持"AI 出意图、算法出手脚、安全层夹中间"的分层架构。
